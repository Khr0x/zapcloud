[package]
name = "zc-executor-core"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

# Sin dependencias de servicio: define el contrato, no lo implementa.
[dependencies]
